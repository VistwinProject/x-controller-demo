# x-controller-demo

**這個 repo 只有編譯輸出,不要在這裡改東西。** 每次都是整包覆蓋。

給業主預覽用的展示版靜態網站,由私有 repo 建置後推上來,
唯一用途是讓 GitHub Pages 有東西可以發佈。

- 展示模式:不連任何後端,畫面上的狀態是預設好的示意資料
- 頂列會顯示 **DEMO** 標記,所有按鈕都不會真的送出任何指令
- `robots.txt` 與 `noindex` 已設,不希望被搜尋引擎收錄

原始碼、開發紀錄與文件都在私有 repo,不在這裡。
